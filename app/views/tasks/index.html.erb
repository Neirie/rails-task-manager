<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<button type="button" class="btn btn-successs"><%= link_to "Add a Task", newtask_path %></button>
